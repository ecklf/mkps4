{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main mkps4 window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
